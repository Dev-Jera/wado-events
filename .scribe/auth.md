# Authenticating requests

To authenticate requests, include a **`Cookie`** header with the value **`"{YOUR_SESSION_COOKIE}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

This API uses session-based authentication. Log in at <a href="/login">/login</a> first — your browser session cookie is sent automatically when using Try It Out.
